# 781 - Source Directory Material Embedding Vector Index Authority Contract Current-Main Sync

## Status

Status: current-main proof/control sync for `source_directory_material_embedding_vector_index_authority_contract`.

Sync doc: `781_SOURCE_DIRECTORY_MATERIAL_EMBEDDING_VECTOR_INDEX_AUTHORITY_CONTRACT_CURRENT_MAIN_SYNC.md`.

Contract doc: `780_SOURCE_DIRECTORY_MATERIAL_EMBEDDING_VECTOR_INDEX_AUTHORITY_CONTRACT.md`.

Contract PR: `#1385`.

Contract branch: `codex/l3-vector-contract`.

Contract branch commit: `759f23346164ed3d052eda60cc8f2375e5eccd89`.

Contract merge commit and current-main checkpoint: `f9c32562f9740fe9375e56d00cd7ce5ecc51fba8`.

Sync branch: `codex/l3-vector-sync`.

Synced result: `current_main_synced_source_directory_material_embedding_vector_index_authority_contract`.

Runtime behavior introduced by contract PR: `false`.

Runtime behavior introduced by this sync: `false`.

## Merge Gate

PR `#1385` merged cleanly after adding `source_directory_material_embedding_vector_index_authority_contract`.

The merge gate was:

- `backend-layer3-api`: `SUCCESS`, `3m14s`;
- `test`: `SUCCESS`, `3m33s`;
- comments totalCount: `0`;
- reviews totalCount: `0`;
- latestReviews totalCount: `0`;
- reviewThreads totalCount: `0`;
- unresolved reviewThreads totalCount: `0`; and
- merge state before merge: `CLEAN`.

## Current-Main Result

Current main now includes `780_SOURCE_DIRECTORY_MATERIAL_EMBEDDING_VECTOR_INDEX_AUTHORITY_CONTRACT.md`.

Current main now records `source_directory_material_deterministic_embedding_vector_index_authority` as the selected contract.

Current main now records `deterministic_local_hashing_vector_embedding` as the selected embedding mode.

Current main now records `deterministic_source_directory_segment_vector_index` as the selected vector index mode.

Current main now records `source_directory_material_deterministic_text_index_authority` and `deterministic_text_segments` as the input authority and vector source.

Current main now records `backend/app/services/layer3_source_directory_vector_index.py` and `backend/tests/test_layer3_source_directory_vector_index.py` as the future owner service and proof test.

Current main now records `source-directory-hash-vector-v1`, `vector_dimensions == 4096`, stable SHA-256 token buckets, token-frequency weights, and L2 normalization as the future deterministic local vectorization parameter identity.

Current main still records no runtime behavior, backend route, API DTO, response model, database model, migration, durable vector-store row write, durable embedding row write, vector database, persistent vector store, vector query, semantic retrieval ranking, RAG execution, context-packet mutation, qualitative generation runtime, prompt/model/provider runtime, hidden LLM planning, package construction, package mutation/reconstruction, package payload rewrite, handoff/export rerun, connector/destination dispatch, real connector invocation, credentials, network egress, provider-public delivery/use, provider-private signed URL generation/use, auth/security broadening, rendered controls, full mockup activation, frontend-durable authority, source expansion, arbitrary recursive ingestion, PDFs, OCR, Office documents, arbitrary binaries, web connectors, raw local path exposure, or source `L3OutputPackage` mutation from the contract or this sync.

## Current-Main Validation

Validation at `f9c32562f9740fe9375e56d00cd7ce5ecc51fba8` passed:

- JSON manifest load;
- `python -m py_compile .\tools\l3-progress-check.py`;
- `python .\tools\l3-progress-check.py`;
- `python .\tools\l3-target-selection-validate.py --expect frozen`; and
- `git diff --check`.

## Next Posture

The next exact current-main posture is `implement_source_directory_material_deterministic_embedding_vector_index_authority_after_contract_sync`.

That next pass may implement only the bounded deterministic local embedding/vector-index service and targeted proof selected by Doc 780, and must still exclude vector retrieval query runtime, RAG execution, prompt/model/provider runtime, persistent vector stores, durable embedding rows, backend routes, package behavior, connector/destination dispatch, provider-public delivery/use, source expansion, network egress, credentials, auth/security broadening, rendered controls, full mockup activation, and frontend-durable authority.
