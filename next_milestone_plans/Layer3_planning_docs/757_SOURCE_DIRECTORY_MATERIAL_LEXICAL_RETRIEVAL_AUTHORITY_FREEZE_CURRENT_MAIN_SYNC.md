# 757 - Source Directory Material Lexical Retrieval Authority Freeze Current-Main Sync

## Status

Status: current-main proof/control sync for `source_directory_material_lexical_retrieval_authority_freeze`.

Doc: `757_SOURCE_DIRECTORY_MATERIAL_LEXICAL_RETRIEVAL_AUTHORITY_FREEZE_CURRENT_MAIN_SYNC.md`.

Freeze doc: `756_SOURCE_DIRECTORY_MATERIAL_LEXICAL_RETRIEVAL_AUTHORITY_FREEZE.md`.

Freeze PR: `#1361`.

Freeze branch: `codex/l3-retrieval-selection`.

Freeze branch commit: `6aa4157bd5194a9b7ebafc80ccd07c6a229dd4e0`.

Freeze merge commit: `8049f131ce35e473864665d05093f1215b6601b1`.

Current-main checkpoint after merge: `8049f131ce35e473864665d05093f1215b6601b1`.

Sync branch: `codex/l3-retrieval-selection-sync`.

Synced result: `current_main_synced_source_directory_material_lexical_retrieval_authority_freeze`.

Runtime behavior introduced by freeze PR: `false`.

Runtime behavior introduced by this sync: `false`.

## Merge Gate

External PR evidence for `#1361` before merge showed:

- state: `OPEN`;
- `backend-layer3-api`: `SUCCESS`;
- `test`: `SUCCESS`;
- comments: `0`;
- reviews: `0`;
- latestReviews: `0`;
- reviewThreads totalCount: `0`;
- merge state before merge: `CLEAN`; and
- head commit: `6aa4157bd5194a9b7ebafc80ccd07c6a229dd4e0`.

After merge, GitHub reported merge commit `8049f131ce35e473864665d05093f1215b6601b1` and state `MERGED`.

## Current-Main Result

Current main now records `source_directory_material_lexical_retrieval_authority` as the selected next retrieval authority after the source-directory deterministic text index runtime sync.

The selected next authority question is `source_directory_material_lexical_retrieval_authority_contract`.

The selected future retrieval mode remains `deterministic_lexical_segment_retrieval`.

The selected future retrieval authority remains `source_directory_material_deterministic_lexical_retrieval_authority`.

The selected future owner service remains `backend/app/services/layer3_source_directory_text_retrieval.py`.

The current-main freeze uses the APS retrieval plane only as deterministic lexical precedent. It does not make `ApsRetrievalChunk` or the APS retrieval plane a source-directory retrieval authority.

## Still Blocked

This sync admits no runtime behavior, backend route, API DTO, model change, migration, source-index durable row writes, vector indexing, embedding generation, retrieval query runtime, qualitative-hybrid analysis runtime, package construction, package mutation/reconstruction, package payload rewrite, source `L3OutputPackage` mutation, handoff/export rerun, connector/destination dispatch, real connector invocation, credentials, network egress, provider-public delivery/use, provider-private signed URL generation/use, auth/security broadening, rendered controls, full mockup activation, frontend-durable authority, hidden LLM planning, arbitrary source ingestion, arbitrary recursive ingestion, broad local upload, PDFs, OCR, Office documents, arbitrary binaries, web connectors, caller-supplied paths/URLs/globs, browser-supplied file bytes, browser/operator path editing, raw local path exposure, or prompt/model/provider runtime.

## Post-Merge Validation

Post-merge current-main sync validation at `8049f131ce35e473864665d05093f1215b6601b1` passed:

- JSON manifest load;
- `python -m py_compile .\tools\l3-progress-check.py`;
- `python .\tools\l3-progress-check.py`;
- `python .\tools\l3-target-selection-validate.py --expect frozen`; and
- `git diff --check`.

## Next Posture

The source-directory lexical retrieval authority freeze is now cleanly synced through current main.

The next exact current-main posture is `write_source_directory_material_lexical_retrieval_authority_contract_before_qualitative_hybrid_runtime`.

Do not implement retrieval runtime, qualitative-hybrid runtime, vector/RAG behavior, package changes, routes, models, migrations, source-index rows, frontend-durable authority, connector/provider behavior, credentials, or network behavior until a later current-main-synced contract admits that exact surface.
