# 917 - Full Mockup Activation Next Blocker Selection

## Status

Status: no-runtime selection/control artifact for `full_mockup_activation_next_blocker_selection_after_source_directory_gate_b_sync`.

Doc: `917_FULL_MOCKUP_ACTIVATION_NEXT_BLOCKER_SELECTION.md`.

Predecessor sync doc: `916_SOURCE_DIRECTORY_MATERIAL_PREVIEW_GATE_B_RENDERED_CONTROL_CURRENT_MAIN_SYNC.md`.

Current-main checkpoint before selection: `b54dfc7cbb8a71b6fad25a852d88877e2190b658`.

Selection branch: `codex/l3-next-blocker-selection`.

Runtime behavior introduced by this selection: `false`.

Rendered behavior introduced by this selection: `false`.

Backend behavior introduced by this selection: `false`.

Route/API/DTO/model/migration/service behavior introduced by this selection: `false`.

Executable test behavior introduced by this selection: `false`.

Production UI behavior introduced by this selection: `false`.

Full mockup program activation selected now: `false`.

Implementation-entry allowed next: `false`.

Selected immediate next pass: `freeze_one_named_full_mockup_activation_target_from_current_main_evidence`.

Selected activation target class: `single_named_server_authoritative_control_or_read_only_projection`.

Selected full-program posture: `blocked_until_all_critical_mockup_operator_journeys_are_live_read_only_excluded_or_blocked`.

## Canonical Source Of Truth

The canonical source of truth for this selection is current `project6-origin/main` at `b54dfc7cbb8a71b6fad25a852d88877e2190b658`, not the dirty root checkout and not a branch-local mockup asset.

The repo-confirmed current-main authority set includes:

- Doc `910_FINAL_FULL_MOCKUP_PROGRAM_READINESS_AUDIT_AFTER_REPRESENTATIVE_SCENARIO_PROOF.md`, which rejected full mockup activation and selected a single existing rendered control extension first.
- Docs `911` and `912`, which froze and implemented the source-directory hybrid context-packet-to-output-handoff rendered status extension.
- Docs `913`, `914`, and `915`, which froze, implemented, and remediated the provider-public URL delivery/use rendered control without admitting raw public URL delivery.
- Doc `916`, which current-main synced PR `#1530` as the bounded source-directory material-preview and Gate B rendered control extension.
- Existing current-main routes and services for source-directory scan/status/material-preview/Gate B, source-directory hybrid output-to-handoff, provider-public prepare/status/revoke/use, internal webhook dispatch/status, package supersession/replacement lifecycle, downstream analysis projection, and read-only mockup projections.

## Decision

The next adequate pass is not full mockup activation and not another inventory-only pass.

The next adequate pass is a freeze for exactly one named activation target that can be proven from current-main route/state/durable authority. The selected target class is `single_named_server_authoritative_control_or_read_only_projection`, not full-program activation.

The freeze must choose one concrete control or projection from live evidence and must classify it as one of:

- `live_server_authoritative_action`, if it posts to an existing server route with durable authority and headed/headless proof;
- `live_read_only_projection`, if it projects existing server state without side effects;
- `intentionally_excluded`, if current product authority says it should not become live; or
- `explicitly_blocked`, if it lacks a route, durable owner, security contract, or testable authority.

## Options Considered

| Option | Current-main evidence | Decision |
| --- | --- | --- |
| Full mockup program activation now | Doc `910` still says full program activation is not ready; Doc `916` preserves full activation as blocked. | Rejected. |
| Another mockup-to-live inventory-only pass | Docs `876`, `889`, `894`, `898`, `902`, `906`, and `907` already classify the critical mockup frame/control families well enough to choose a target. | Rejected unless current-main drift appears. |
| Another source-directory rendered control pass | Source-directory scan/status, material-preview, Gate B, hybrid status, and delivery-status controls are already current-main represented by Docs `842`, `843`, `911`, `912`, and `916`. | Deferred unless a concrete source-directory defect appears. |
| Internal webhook status panel | Docs `871` through `875` already selected, froze, implemented, and current-main synced the read-only internal webhook rendered status surface. | Not selected. |
| Downstream analysis environment projection | Docs `860` through `864` already selected, froze, implemented, and current-main synced the read-only session-summary projection. | Not selected. |
| Provider-public URL rendered use control | Docs `913` through `915` already landed the bounded use-decision control and freshness remediation. Raw delivery remains blocked. | Not selected for immediate next. |
| Package mutation or replacement expansion | Current main has package supersession/replacement routes and rendered lifecycle controls, but full mutation/reconstruction expansion still needs a named product action, payload/identity contract, rollback/idempotency policy, and leakage proof. | Valid future target only after a freeze names one package action. |
| Connector/destination dispatch expansion | Internal webhook is admitted; arbitrary connector/destination dispatch and `ConnectorRun`/`ConnectorRunTarget` creation remain blocked without a named destination, credential policy, and egress contract. | Valid future target only after a freeze names one destination mode. |
| Broad RAG/vector/model/provider runtime | Current main has deterministic source-directory vector/hybrid authority, but broad semantic/RAG/model/provider behavior remains blocked without exact index authority, query-set authority, reproducibility, and operator-visible state. | Valid future target only after a freeze names one analysis mode. |
| Browser/full-program durable behavior | Browser proof is required for rendered activation, but browser storage cannot become durable authority. | Keep as proof layer, not activation authority. |
| Auth/security hardening | Needed before any broad activation, but current main has no selected named security behavior, protected surface, threat model, or policy owner for this specific next pass. | Defer until a named surface is selected. |

## Required Future Sequence

1. Freeze exactly one named activation target from current-main evidence.
2. Name its canonical source of truth, route/API contract, state object, durable owner, DOM selector if rendered, proof tests, leakage guard, and no-go surfaces.
3. Implement only the frozen target if the freeze remains valid after current-main refresh.
4. Prove it with isolated runtime state; use headed and headless Chromium for any rendered control or projection.
5. Current-main sync the proof only after local validation, GitHub checks, PR comments, PR reviews, latestReviews, and reviewThreads are clean.
6. Repeat the same freeze, implementation, proof, and sync cycle for any remaining named source/package/connector/provider/RAG/browser/auth blocker that product authority actually selects.
7. Run a full mockup activation readiness re-audit after the selected blocker lanes land.
8. Produce a screen/control matrix that classifies every critical mockup operator journey as live, read-only, intentionally excluded, or explicitly blocked.
9. Freeze full mockup program activation only if the re-audit has no unresolved route/state/durable-authority/headed/headless/security gaps.
10. Implement full mockup activation only as the frozen scope and only after the full-program freeze passes review.

## What Is Still Required Before Full Mockup Activation

- Every action-capable mockup control needs a server route, request/response contract, durable authority owner, negative invariant tests, and headed/headless proof.
- Every read-only mockup panel needs a server-owned state source, stale/unavailable state, redaction policy, and browser proof if rendered.
- Every intentionally excluded control needs an explicit product or authority reason, not silent omission.
- Every blocked control needs the exact missing blocker named: route, state, durable owner, security policy, test harness, product authority, or external dependency.
- Source expansion beyond admitted server-configured ingestion must remain blocked unless separately frozen.
- Package mutation/reconstruction must remain blocked unless one package action and payload identity policy are frozen.
- Connector/destination dispatch must remain blocked unless one named destination mode and egress/credential policy are frozen.
- Provider URL delivery must remain blocked beyond the bounded rendered use decision unless raw delivery/public proxy/provider-private semantics are separately frozen.
- Broad RAG/vector/model/provider runtime must remain blocked unless one deterministic analysis mode is selected.
- Browser storage and frontend-only durable authority must remain blocked as authority sources.
- Auth/security changes must be frozen around one protected surface before broad activation.

## Next Posture

Next exact posture: `freeze_one_named_full_mockup_activation_target_from_current_main_evidence`.

Do not implement full mockup activation, broad source/package/connector/provider/RAG/browser/auth behavior, mockup-frame write controls, or frontend-only durable authority from this selection alone.
