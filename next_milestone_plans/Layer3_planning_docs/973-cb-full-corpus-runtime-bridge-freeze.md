# 973 - Candidate B Full-Corpus Runtime Bridge Freeze

## Purpose

Admit the smallest governed bridge that can turn the validated Candidate B 69-PDF full-corpus runtime triplet into Layer 3 material authority.

This is an admission/freeze artifact only. It introduces no runtime, route, DTO, model, migration, rendered UI, parser, provider, connector, auth/security, source-expansion, RAG/vector/model, browser-storage, frontend-only durable-authority, or full-mockup behavior change.

```yaml
milestone: candidate_b_full_corpus_runtime_to_layer3_material_authority_v1
current_main: df9a37423274f3f0590b2e943c39aaffda0d91d7
predecessor_checkpoint: 972-cb-full-corpus-triplet.md
admission_result: admitted_for_next_runtime_implementation
admitted_bridge_mode: candidate_b_full_corpus_runtime_to_layer3_material_authority_v1
admitted_compare_evidence: aps.full_corpus_compare_triplet_validation.v1
validated_triplet_hash: 1052eea1153d6fdb21abd18384abc5c2db73497c9d34f18ecf52239f71c82a2f
baseline_run_id: 7958ca0c-d163-4c6e-a0bf-2cac4e4bfe20
candidate_a_run_id: 9b09f014-95f9-41cb-820c-8f5296a993bc
candidate_b_run_id: f644b3f6-a7a9-4889-84d9-d842f5d12e79
implementation_in_this_freeze: false
```

## Authority Inputs

- Current main `df9a37423274f3f0590b2e943c39aaffda0d91d7`.
- `972-cb-full-corpus-triplet.md`.
- `tools/validate_full_corpus_triplet.py`.
- `backend/app/services/layer3_candidate_b_runtime_bridge.py`.
- `backend/app/services/layer3_candidate_b_downstream_proof.py`.
- Existing fixture-scoped runtime bridge freeze `965-cb-runtime-bridge.md`.

## Current-Main Gap

Current main has:

- Candidate B first-class completion audit;
- Candidate B eligible-PDF default selection;
- Candidate B visual-lane admission;
- Candidate B bundle bridge;
- Candidate B fixture/workbench-scoped runtime bridge;
- Candidate B full-corpus processing proof;
- Candidate B full-corpus baseline / Candidate A / Candidate B compare triplet proof.

Current main does not yet have:

- a bridge mode that accepts the validated 69-PDF full-corpus triplet as compare authority;
- Layer 3 material authority over the full-corpus Candidate B runtime target set;
- downstream proof over the full-corpus bridge receipt.

## Admitted Bridge

The next implementation may add bridge mode:

```text
candidate_b_full_corpus_runtime_to_layer3_material_authority_v1
```

The bridge may accept:

- `candidate_b_run_id`;
- `baseline_run_id`;
- `candidate_a_run_id`;
- an optional explicit full-corpus validation receipt or an internally recomputed validation over same-checkout receipts;
- existing operator confirmation and client request id fields used by the current Candidate B runtime bridge.

The bridge must fail closed unless:

- all three run ids are discoverable in same-checkout runtime discovery;
- baseline and Candidate A summaries use `document_processing_engine: baseline`;
- baseline and Candidate A request config proves `document_processing_engine_explicit: true`;
- Candidate A uses `visual_lane_mode: candidate_a_page_evidence_v1`;
- Candidate B uses `document_processing_engine: candidate_b_opendataloader_pdf`;
- Candidate B uses `visual_lane_mode: candidate_b_opendataloader_page_evidence_v1`;
- all three runs have 69 target outcomes and 69 `recommended` targets;
- all three runs share the same ordered accession set;
- all required local-corpus validate-only gates passed;
- Candidate B has non-empty ordered-unit evidence, visual/page evidence, and retained source-PDF refs;
- no selected Candidate B material target lacks normalized text authority.

## Material Authority Scope

The bridge may materialize only server-owned, source-directory-compatible JSON/MD material for the validated full-corpus Candidate B target set:

- redacted Candidate B runtime summary JSON;
- redacted full-corpus compare target set JSON;
- redacted Candidate B runtime trace JSON per admitted target;
- normalized text JSON per admitted target;
- Markdown text material per admitted target.

The bridge must include deterministic hashes for:

- full-corpus compare target set;
- Candidate B runtime summary;
- admitted material file subset;
- runtime storage/review-root authority;
- governed retained artifact family;
- final bridge receipt.

## Retained Artifact Family

PDFs, annotated PDFs, extracted images, visual refs, runtime trace documents, and delivery/inspection artifacts remain retained Candidate B evidence/product artifacts. They are not excluded from Candidate B governance. They are not automatically text-material payloads for this bridge unless separately admitted.

The bridge must preserve retained artifact-family metadata and operator-inspection provenance without exposing raw local paths, raw URLs, provider keys, credentials, or local roots.

## Compatibility Requirement

The bridge output must be consumable by the existing Layer 3 source-directory material preview and Gate B material authority without widening source-directory policy.

The implementation may reuse existing bridge receipt and curated-root mechanics, but it must not route full-corpus comparison through `compose_workbench_compare_targets` or the fixture manifest. Workbench fixture parity remains covered by `candidate_b_runtime_source_to_layer3_material_authority_v1`.

## Downstream Proof Requirement

After the bridge lands, the next proof must show Candidate B full-corpus material authority can flow through the existing Candidate B runtime downstream proof path or a separately admitted full-corpus proof mode covering:

- source-directory scan;
- material preview;
- Gate B;
- retrieval/context;
- qualitative analysis;
- package/review;
- handoff/export;
- same-origin delivery;
- provider-private redacted lifecycle where admitted;
- internal webhook where admitted;
- status/projection evidence;
- retained artifact-family inspection.

## Negative Invariants

- Do not change Candidate A semantics.
- Do not remove baseline rollback or fail-closed behavior.
- Do not broaden Candidate B default beyond eligible/effective PDFs.
- Do not ingest arbitrary PDFs/images into Layer 3 text-material analysis.
- Do not ingest the runtime DB or broad storage tree as source material.
- Do not use fixture-manifest workbench target selection for the full-corpus bridge.
- Do not add provider object writes, arbitrary connector dispatch, broad source expansion, RAG/vector/model runtime, auth/security changes, browser-storage authority, frontend-only durable authority, or full mockup activation.
- Do not expose raw local paths, raw URLs, provider keys, local roots, or unredacted artifact refs in rendered/operator surfaces.

## Next Exact Posture

After this freeze lands, implement:

```text
candidate_b_full_corpus_runtime_to_layer3_material_authority_v1
```

The implementation should be a narrow extension of the existing Candidate B runtime bridge, not a replacement of the fixture/workbench bridge and not a broad source-ingestion expansion.
