# G1 owner delta packet — dual-live M0 (adjudicated 2026-07-30)

3-Opus adjudication (textualist/mechanist/governance) + Fable synthesis of the Codex-vs-Fable S1-S4
dispute. NET: 1 required frozen-spec amendment (S3), 3 reading-confirmations (S1, S2-NRC, S4), 0
owner action on S2-ScienceBase/Task-4 (agent-executable). G1 disposition upheld honest+scope-clean.

---

## OWNER DECISIONS

# M0 dual-live frozen-spec delta packet — owner decisions only\n\nFrozen plan: `docs/superpowers/plans/2026-07-29-dual-live-proof.md` (M0 freeze c7b47543). Adjudicated: Codex STOP items S1-S4 + the Task-4 persistence gap, against the frozen bytes, by three independent Opus angles reconciled on evidence. Net: **one required amendment (Decision 1) and three reading-confirmations (Decisions 2-4)**. Everything else is agent-executable and listed separately — nothing below asks you to approve implementation work.\n\n---\n\n## Decision 1 — REQUIRED frozen-spec amendment: S3 predicate/receipt phase circularity (the sole true frozen-contract defect)\n\n**The defect, in the frozen bytes.** ScienceBase arming is gated on the NRC predicate: "Only after the predicate passes, create the ScienceBase parent arming" (2841). Predicate clause 5 (1008-1011) requires: "the raw SHA-256 recorded on the canonical connector-target receipt equals both the rederived ledger's artifact-completion `body_sha256` and the matching counter record's decoded-body SHA-256." But the plan defines exactly one receipt, and it is a Phase-B artifact: "persist one canonical origin receipt on the connector target" (28); "one connector-target receipt is canonical; all downstream copies are receipt-ID/hash projections" (invariant 12, 98-99); "exactly one canonical receipt lives on the connector target" (1992-1993, authoritative copy at `ConnectorRunTarget.source_reference_json["connector_origin_receipt_v1"]`, 2062-2063). Its only minting path is Task 6: "`derive_connector_origin_receipt` loads run/target/provenance/version/linkage" (2026) — and for NRC, "`ApsContentLinkage.blob_sha256` binding therefore occurs in the downstream phase" (1925-1927), which launches at execution-order step 9 (2864-2865), AFTER step 5's arming gate. So the gate requires a value from a receipt that cannot exist yet. Verified: the word "receipt" as a persisted connector artifact appears nowhere in Tasks 4/5 (nothing between lines 1506 and 1960) — no Phase-A receipt is ever minted. No agent may route around this: "no wrapper, caller, or execution-order step may substitute a weaker check" (980-981). Codex's STOP here was correct discipline.\n\n**Your options (ranked):**\n\n- **Option B — reword clause 5's referent (recommended, minimal delta).** Amend clause 5 (1008-1011) so the third leg of the equality is the raw SHA-256 **rehashed from the content-addressed blob** (recorded as `ConnectorRunTarget.downloaded_sha256`), equal to the rederived ledger `body_sha256` and the counter decoded-body SHA-256. Consequential edits: the falsification bullet at 845-846 and the `NrcAcquisitionSuccessEvidence` field naming at 1013-1014. Invariant 12 stays true untouched (one canonical receipt = the Task-6 origin receipt). Not a weaker check — phrase it as a blob REHASH, not a trust-the-stored-column read (preserving 984-986's never-trust-projections discipline), triangulated against the same two independent rederivations; arguably stronger than "recorded on ... receipt." Task 8's evaluator (2427-2433) still independently re-verifies NRC-first sequencing end-of-campaign, so no auditability is lost. Smallest frozen-text blast radius; fits your standing anti-churn rule.\n- **Option A — Codex's split-receipt (close second).** Introduce a minimal Phase-A NRC acquisition-sequencing receipt (raw hash + terminal-ledger + authority/grant bindings only, no linkage/provenance/parsing) as clause 5's referent, keeping the full Phase-B connector-origin continuity receipt for downstream. Cost: invariant 12's "one connector-target receipt is canonical" (98-99) must be re-tuned to a two-receipt model, plus vocabulary at 845-846, 1008-1011, 1013-1014, and 2427-2429 — a wider frozen-text surgery. Choose this if you want the Phase-A sequencing proof to be a durable receipt artifact in its own right. Note: two of the three Opus adjudicators leaned A; I rank B first on blast radius and anti-churn — this ranking is your judgment call, both options are fail-closed.\n- **Option C — mint linkage/provenance/receipt before ScienceBase arming — REJECT (unanimous).** It drags document parsing into the still-live acquisition process, violating invariant 21 (124-128: the acquisition-only child "never parses them beyond the bounded admission media/shape checks").\n\n**Decision needed:** pick A or B (C rejected). Until granted, Tasks 6-9 receipt/evaluator/gate work stays blocked; the agent-executable substrate work (see separate list) can proceed in parallel.\n\n---\n\n## Decision 2 — Reading-confirmation: S1 finalize lease semantics (no text change)\n\n**Claimed defect (Codex):** finalize requires an unexpired lease, so an expired-lease run strands in `running`, contradicting "every strict stop terminates `failed`."\n\n**The bytes:** finalize's guard (1089-1091) lists only "primary key, `status == \\"running\\"`, and the exact active lease token" — no unexpiry term. The reservation guard (1284-1289) lists "the exact active lease token" AND separately "`grant.issued_at <= now < grant.expires_at` plus an unexpired lease" — if "active" meant "unexpired," the extra conjunct would be redundant. And finalize itself "clears or expires the execution lease in the same transaction so no unexpired lease survives the terminal transition" (1092-1093) — i.e., it is designed to run on a possibly-already-expired lease. Decisively: under Codex's reading, an expiry-triggered stop could never finalize, which would itself violate "every strict stop terminates `failed` the same way" (1592-1594; also 1743-1745) since generic finalize is unreachable for strict runs (1099-1104). The token-identity reading is the only self-consistent one.\n\n**Confirm:** "exact active lease token" = token-VALUE identity match; unexpiry is required only on the send/reservation path (1289), never as a finalize precondition. Post-expiry: sends blocked, finalize-`failed` reachable. One-line ratification; no frozen-text change.\n\n---\n\n## Decision 3 — Reading-confirmation: S2-NRC linkage timing (no text change)\n\n**Claimed defect (Codex):** Task 5 acceptance (1741-1742: "successful bytes bind `ConnectorRunTarget.downloaded_sha256`, `ApsContentLinkage.blob_sha256`, and the content-addressed blob") demands Phase-A linkage, contradicting the raw-only acquisition rule.\n\n**The bytes:** the plan resolves this itself three paragraphs of the same task later: "`ApsContentLinkage.blob_sha256` binding therefore occurs in the downstream phase against the admitted bytes; the raw-hash equality it must prove is unchanged" (1925-1927). 1741-1742 states the eventual binding TARGETS (three artifacts, one raw hash); 1925-1927 fixes the TIMING (Phase B), consistent with invariant 21.\n\n**Confirm:** 1925-1927 governs timing — Phase A binds `downloaded_sha256` + blob; Phase B binds `ApsContentLinkage.blob_sha256` to the same hash. No frozen-text change. (This confirmation is also the fact underlying Decision 1's circularity — the two are consistent.)\n\n---\n\n## Decision 4 — Reading-confirmation: S4 skip semantics of line 2639 (no text change; optional micro-clarification available)\n\n**Claimed defect (Codex):** any per-test `skipped` in any Task-9 command makes the green-suite claim unachievable.\n\n**The bytes:** the only "skip" sentence in the plan is 2639: "Do not claim a full green suite if any command is skipped or unavailable. Record exact command, exit code, and collected/passed/failed counts." Its subject is "any COMMAND" — a prescribed command not run, or unrunnable. The test-level reading is unsatisfiable at the frozen baseline itself: the plan's own Step-4 command (2633: `python -m pytest tests -q -k "layer3 or connector or nrc"`) sweeps ~3,240 pre-existing tests carrying 14 pre-existing environment-conditional skips (Codex's V8) that no G1 work could remove — a reading the plan's own command violates on day one cannot be the contract. Codex's V2 1-skip is its own guard-test design; a launcher + non-collected guarded module yields zero top-level skips with no spec change. V4/V5 being genuinely "unavailable" (missing Task-8 test files) is a correct 2639 trip caused by unbuilt work, cured by building Task 8 — not by amending the spec.\n\n**Confirm:** 2639 is command-level — every prescribed command must run and be recorded; pre-existing environment-conditional test skips inside a command that runs do not void the green claim; net-new G1 tests must not introduce skips (the restructure obligation covers this). **Optional** (decline is fine under anti-churn): a one-clause clarification of 2639 distinguishing "prescribed command not run/unavailable" from named expected in-test skips. Not required for correctness.\n\n---\n\n## For awareness — no decision required\n\n- Codex introduced net-new flag `CONNECTOR_LIVE_EGRESS_EXCLUSIVE_PROOF_MODE` defaulting True — a protective tightening consistent with default-off egress; benign, in scope.\n- Codex report's V4/V5 exit code "1" should be 4 (pytest usage error on a missing file) — cosmetic, to be corrected in the successor report.\n- "Independently approved" in the Codex report means same-session Codex subagents — a real STOP was produced for Tasks 6-9, but it is not external review; this adjudication supplies the external pass.\n- Frozen docs verified byte-untouched by G1; no push/egress/grant activity claimed or found by any adjudicator.
   

---

## AGENT-EXECUTABLE (no owner decision — follow-up Codex lane)


- ScienceBase Phase-A persistence (S2-SB half = Task-4 gap): complete the strict ScienceBase lane per frozen lines 1590-1591 ("a complete `200` stores raw bytes, target SHA-256, provenance, and `DatasetVersion.content_hash` with equality") — persist DatasetVersion.content_hash, DatasetSourceProvenance, and connector-source-intake rows under the 1578-1584 safe-field whitelist with URL scalars null. Invariant-21-legal (content-addressing, not parsing). MUST land BEFORE Task-6 receipt minting: derive_connector_origin_receipt (2026) loads provenance/version/intake that currently do not exist; Codex's proposed forward sequence (report line 286) omits this predecessor.
- NRC Phase-B linkage binding: bind ApsContentLinkage.blob_sha256 in the downstream phase against the admitted bytes per 1925-1927, before NRC origin-receipt derivation — prerequisite input to derive_connector_origin_receipt for the NRC target.
- De-couple the Phase-A predicate: remove the calls to derive_connector_origin_receipt / assert_connector_origin_continuity from inside evaluate_nrc_acquisition_success (Codex report line 146 — a coupling the frozen predicate definition at 977-1011 never specified); implement clause 5 against the amended referent once owner Decision 1 lands. The rest of clauses 1-4 reference only Phase-A artifacts and need no change.
- Guard-test restructure (S4): convert the guard test to a launcher plus a non-collected guarded module so every prescribed Task-9 command reports zero top-level skips (eliminates the V2 "138 passed, 1 skipped"); record exact command, exit code, and collected/passed/failed counts per 2640 for every command including the broad Step-4 sweep.
- Build Task 7 (production writer for connector_origin_receipt_v1 mint/store + continuity assertion at enforcement points) and Task 8 (sealed campaign-log capture, independent evaluator, validate-only runner), creating tests/test_campaign_log_capture.py, tests/test_dual_eval.py, tests/test_dual_gate.py so the currently-unavailable V4/V5 commands become runnable. Receipt-shape details of Tasks 6-8 wait on owner Decision 1; the log-capture and non-receipt evaluator scaffolding do not.
- Corrected forward sequence for the follow-up Codex lane (replaces report line 286): (1) ScienceBase Phase-A provenance/DatasetVersion/intake persistence -> (2) Task-6 receipt mint/store per the granted S3 amendment (NRC linkage bound in Phase B first) -> (3) Task-7 continuity -> (4) Task-8 sealed log/evaluator/validate-only runner -> (5) exact Task-9 gate re-run with per-command recording. Run S1 under the confirmed token-identity reading (remove any stranded-running path); state the anti-churn/realistic-reviewer rule in the dispatch per standing owner feedback.
- Successor-report hygiene: correct the V4/V5 exit-code claim from 1 to 4 (pytest usage error) and restate the S1-S4 items under the adjudicated framing (1 spec defect, 3 confirmations, 2 build gaps) so the record does not propagate the 4-contradictions overcount.
  