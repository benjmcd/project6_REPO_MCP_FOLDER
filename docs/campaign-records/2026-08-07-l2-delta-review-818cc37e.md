# L2 Delta-Review Record — defect-#2 transport fix landing (b580cddc..818cc37e)

**Verdict: L2-SOUND-WITH-FINDINGS.** L3 authority regeneration is UNBLOCKED on this axis, subject to the conditions below.

## 1. Delta identity

- Branch: `codex/dual-live-plan`; remote tip verified `git ls-remote project6-origin refs/heads/codex/dual-live-plan` = `818cc37e2f626bf8bb46056674d302df2765c7f0`, equal to worktree HEAD (`worktrees/dl-land2`, branch `dl-land2-work`).
- Single commit, single parent: `git rev-list --parents -n 1` = `818cc37e... b580cddc05c90219db8b3c070a4a05fb9f5f9110` — exactly the expected base, no merge/rebase artifact.
- Commit message trailer-free (no Co-Authored-By, no AI attribution); author/committer benjmcd.
- Shape (`git diff --numstat b580cddc..818cc37e`): **3 files, +672/−11**
  - `backend/app/services/connector_egress_transport.py` +45/−10 (M)
  - `backend/tests/test_connector_transport_loopback.py` +623/−0 (A)
  - `tools/dual_live_run.py` +4/−1 (M)

## 2. What the delta does (adjudicator-verified against the diff)

1. `_set_raw_socket_timeout` (bool) → `_arm_raw_socket_timeout` (tri-state `armed|no_socket|failed`); a candidate that rejects arming no longer short-circuits the walk (`continue` replaces `return False`).
2. Read loop: FAILED or a second unarmed pass refuses **before** any read (the previously fail-closed case is not loosened); exactly one unarmed read is admitted only in the first NO_SOCKET state (`unarmed_read_spent` latch); an empty unarmed read is the clean released-connection break → `completed`.
3. Data on an unarmed read forces `transport_error` and the terminal raises the **distinct code** `connector_egress_transport_post_release_unarmed_data` (body never returned to the caller; counter record carries counts + sha256 only, never bytes).
4. Runner refusal widening: exactly one code — `dual_live_quiescence_failed` — promoted into `_PUBLIC_REFUSAL_CODES` (`tools/dual_live_run.py:27-31`). The code pre-exists at the base (`git grep` at b580cddc: `dual_live_runtime.py:3417`); no new code invented. `_refuse` emits a fixed ASCII literal only on frozenset membership, else the opaque `dual_live_run_refused` — no interpolation, no attacker-chosen string.

## 3. No gate-allowlist widening — explicit statement

**No allowlist, gate definition, or certified-surface manifest was widened, narrowed, or edited by this delta.** Adjudicator-verified: `tests/test_dual_gate.py` is byte-identical at both endpoints (blob `4573ee88d628445d861682adc5161822021bab2f` at b580cddc and 818cc37e). Both production files were **already members** of the certified surface at the base: `backend/app/services/connector_egress_transport.py` at `tests/test_dual_gate.py:98` (ALLOWED_CHANGED_PRODUCTION_PATHS, opened :91), `tools/dual_live_run.py` at :82 (ALLOWED_NEW_PRODUCTION_PATHS, splatted :93). The new test file lands in `backend/tests/`, outside the gate's governed production surface. Anchors intact and unchanged (not merely present): frozen plan blob `68f740af86dc7d1ac2227f81a6ea28e7e2c7458f` identical at both endpoints; pilot seal `b8a89df28ed1ed5adfd8ded7ee12d28863cf0ed2` (= blob of `backend/tests/test_layer3_connector_source_intake_pilot.py`, identical at both endpoints; asserted at `tests/test_dual_gate.py:293`, pinned at `test_layer3_connector_vertical_loop.py:194`); `backend/tests/test_egress_transport.py` blob identical (`03f64d18...`).

## 4. Re-argued review posture — socket-boundary change (NOT inherited from C2-DELTA-SOUND)

The defect-#1 C2-DELTA-SOUND posture was argued for an import-ordering change, statically decidable; it does not transfer and was not relied on. For this delta the offline/key-free/no-egress posture is re-earned on independent grounds, which the adjudicator accepts after spot-checking the load-bearing legs first-hand:

1. **The behavior under review is a client-stack-internal state transition**, not peer-dependent: NO_SOCKET arises because urllib3 `release_conn` sets `_connection = None` and http.client clears `fp` once the declared Content-Length body is consumed (verified by L2-SEC against the pinned interpreter's own sources at C:\p6-run\py312, urllib3 2.7.0 — producer-independent, but adjudicator did not re-open those sources; the in-code rationale comment cites the same lines). A 127.0.0.1 peer reproduces the exact transition.
2. **The send_callable-is-None branch is genuinely exercised on a real socket** — adjudicator-verified: constructor declares `send_callable: Callable[..., Response] | None = None` (:1244) assigned to `self._send_callable`; every loopback transfer constructs the transport with only `connector_run_id`/`lease_token`/`arming_fingerprint`/`counter_path`, so `session.send` is the live path through the unmodified adapter → urllib3 → http.client → socket stack over a real ephemeral 127.0.0.1 TCP listener.
3. **Non-vacuity is asserted, not hoped**: `assert unarmed, "expected a post-release unarmed pass; branch went untested"` for each body size that should produce one; `assert not any(_is_failed(...))`; armed-candidate identity pinned to `{_CONNECTION_SOCK_CANDIDATE}` (= 1, `connection.sock`); DNS stub asserted to be the resolver actually consulted (`dns_calls == [(shape.host, 443)]` — no live lookup); autouse urllib3 2.7.0 version pin fails the suite on runtime drift; ledger-ineligibility seam asserted exactly so any other derivation failure still fails the test.
4. **Bounding guarantee survives every path** (L2-SEC walked all seven loop exits; adjudicator re-read the diffed loop): deadline-first break unchanged; ARMED socket-bounded; FAILED refuses before read (strictness preserved); first NO_SOCKET admits one read that on the only reachable real path returns `b""` instantly from the released reader; non-empty unarmed data is refused with the distinct code, volume bounded at one 65,536-byte chunk; second unarmed pass refused and additionally structurally unreachable (both post-read paths break). Two backstops outside the patch: urllib3 arms the socket with the requests read-timeout before body read, and `enforce_content_length` turns truncation into `IncompleteRead` → `transport_error`, so no path converts an unbounded or truncated read into a PASS.
5. **Credential-path neutrality**: L2-SEC's filtered grep over the delta lines returned one false positive ("url" inside the comment word "urllib3"). The delta touches no env var, header construction, credential attachment, audience binding, TLS verification, redirect/retry handling, URL validation, or redaction surface. All `_fail` codes are fixed ASCII literals. Loopback tests run key-free with zero egress.

**Enumerated live residuals (fail-closed by design, NOT certified here):** TLS/`ssl.SSLSocket` as arming candidate 1 unexercised (disclosed divergence, cleartext-only loopback); chunked or connection-close framing from real endpoints could reach a different branch; end-to-end quiescence/evidence-chain at real timing unproven. Systemic expectation stands: two consecutive first-real-exercise failures precede this; a third at the live frontier is plausible — the design refuses rather than false-PASSes on every enumerated surprise.

## 5. Findings carried (all adjudicator-reviewed; none blocking)

- **F1 (minor, custody)** — CI-shard coverage debt +1: `test_connector_transport_loopback.py` matches no `BACKEND_SHARD_PATTERNS` entry (adjudicator-verified: no `test_connector_*` pattern exists) and is not in `EXCLUDED_BACKEND_TESTS`, so `test_backend_test_files_are_ci_covered_or_allowlisted` gains one more uncovered entry. Honest framing verified: the gate was already red branch-locally (dual/egress/campaign family files exist at the base and match no pattern; the family is absent from `project6-origin/main`), so this is pre-existing merge debt +1, not a flipped gate. Must be paid before merge to main (the completeness test is itself a required release gate).
- **F2 (note, evidence-record)** — RED-count inconsistency, adjudicator-CONFIRMED with refinement: the three tri-state helper tests AND the distinct-code test carry `@tri_state` skipif, so a full-module run at the unpatched revision yields **3 failed** (nrc_multi_chunk, nrc_single_chunk, sciencebase_multi_chunk — each asserts `completed` where the old guard refuses), 5 skipped — not 1 failed. The packet's "RED 1-failed-at-unpatched" was therefore necessarily a scoped single-node invocation. Direction is toward MORE demonstrated defect coverage; record the scoping, do not treat the packet count as a full-module result.
- **F3 (note, security-adjacent)** — The `continue`-on-rejection widening of ARMED classification depends on real-stack candidate identity (candidates 1 and 3 are the same socket; candidate 0 exposes no `settimeout`); pinned only by the loopback armed-candidate assertion. A future candidate-walk edit must not be made blind to this dependency.
- **F4 (note)** — On the unarmed-data break, the streaming-cap/aggregate-budget check is skipped for the admitted chunk; bounded at one chunk, both outcomes refuse before any response is returned, accounting truthful (length+sha256 only). Classification nuance, not a bounding violation.
- **F5 (note)** — Loopback fixture carries the literal dummy header value `"secret"` (`Ocp-Apim-Subscription-Key`), rendered into pytest output on head-assertion failure. Harmless as landed (127.0.0.1-only, ASCII literal); **never substitute a live key into this fixture**.
- **F6 (note)** — The new distinct code is deliberately NOT in `_PUBLIC_REFUSAL_CODES`; at the run boundary it flattens to `dual_live_run_refused`. Correct fail-closed default — but a future refusal of this shape must be diagnosed from captured campaign logs, not the wrapper output. `test_pinned_urllib3_release_semantics` verifies only the version pin (name over-promises; cosmetic).
- **F7 (note, operational)** — `dl-land2` is checked out with `core.autocrlf=true`; `tools/dual_live_run.py` carries CRLF on disk (blob clean — custody unaffected, `tr -d '\r'` reproduces the blob sha256). The recorded lane landmine requires `dual_live_run.py` LF-on-disk and `lock.txt` CRLF at run time. **Do not perform L3 authority regeneration from this worktree**; use a purpose-prepared run tree per the recorded recipe.

## 6. Producer-asserted vs independently-verified

**Independently verified (adjudicator, read-only git in the worktree):** HEAD/remote tip identity; single-commit parentage; 3-file +672/−11 shape; gate blob identity across endpoints; allowlist membership at both endpoints; frozen blob + pilot seal + egress-test blob identity; full production diff content (tri-state helper, loop classification, terminal code selection); refusal-set widening of exactly one pre-existing code and the `_refuse` bare-literal property; constructor `send_callable` default None; loopback transfer construction sites; `@tri_state` skip guards and RED arithmetic; non-vacuity assertions; static node count = 10 (1+2+1+1+4+1); CI-shard pattern/allowlist non-membership of the new file; trailer-free commit message; refusal code pre-existence at base.

**Verified by reviewers, accepted with named basis (not re-derived by adjudicator):** byte custody sha256 equalities landed-blob = reviewed-source-tree = durable carrier (L2-CUSTODY, exact digests recorded); pinned-interpreter source citations for urllib3 2.7.0 / http.client release semantics (L2-SEC, read from C:\p6-run\py312 itself); evidence-tree quiescence (no post-landing writes under C:\p6-run; dl-run2 clean at e53955d2).

**Producer-asserted, NOT independently re-executed (read-only rule bars pytest):** counters **gate 356, census 404, loopback 10, egress 92** at 818cc37e. Consistency checks that could have falsified them did not: egress test file byte-identical across the delta (92 cannot have moved); census is a fixed-file measurement of `backend/tests/test_dual_eval.py` (per the campaign record), so 404 is correctly invariant despite the new module's "connector" basename; static loopback node count corroborates 10.

## 7. NOT certified by this review

- **No G2-P8 authorization** — a fresh, non-inheritable P8 remains a separate owner act.
- **No live-run authorization** — NRC grant 5c4cfe20 remains SPENT (never re-issue); SB grant b51cc632 remains stranded; no credential issuance or use is sanctioned here.
- **The real workload has still never completed end-to-end** — the live HTTPS credentialed path (TLS arming shape, real endpoint framing, real timing quiescence) is unexercised; this record certifies the fix's offline-provable properties only.
- **Counters are producer-run** — gate 356 / census 404 / loopback 10 / egress 92 rest on the landing executor's execution, corroborated but not reproduced.
- **No merge-to-main readiness** — F1's completeness-gate debt (15 uncovered branch-local files) must be paid before any merge.
- **L3 authority regeneration itself** — unblocked, not performed; it must run from a purpose-prepared run tree (F7), not dl-land2.
