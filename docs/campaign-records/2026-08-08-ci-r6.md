# Lane B CI R6 first-green record

Date: 2026-08-08

Status: `R6_FIRST_REMOTE_GREEN_SECOND_CONFIRMATION_REQUIRED`

## Authority boundary

This record covers only remote verification of the CI-debt correction lane on
draft PR #2485. It does not issue P8, a lease, credential access, egress,
launch, merge, ready-for-review, Lane A, or any owner-only act.

## Exact-head remote evidence

GitHub Actions run `31296855809` evaluated exact PR head
`f6ce353336c05491bc5fa9c3da5667974fc83d68` and completed successfully.

- `dual-gate-windows` passed in 16m28s. Its first serial command reported 358
  passes in 258.47 seconds; its separate evaluator process reported 404 passes
  in 604.24 seconds.
- All four backend shards and their aggregator passed.
- All four root shards and their aggregator passed.
- All four Playwright shards and their aggregator passed.
- Backend coverage, Postgres migrations/3C, release lock, NRC APS OCR, SEC XBRL
  Arelle provisioning, and `release-gate` passed.
- No failed job was retried.

This is the first complete green run after R5. It proves the R4/R5 hosted-CI
corrections on this exact head, but the existing acceptance rule requires a
second consecutive code-equivalent full workflow run before stable remote
closure is recorded.

## Required next evidence

The next commit may add only this evidence record or later closure records; it
must leave every executable, workflow, dependency, and test byte from
`f6ce353336c05491bc5fa9c3da5667974fc83d68` unchanged. A fresh full workflow on
that code-equivalent child must pass every required job, including
`dual-gate-windows`, backend coverage, and `release-gate`.

Until then, the CI-debt status remains
`FIRST_REMOTE_GREEN_SECOND_CONFIRMATION_REQUIRED` and the PR remains draft.
