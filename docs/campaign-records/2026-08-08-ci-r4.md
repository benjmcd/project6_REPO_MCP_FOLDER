# Lane B CI R4 correction record

Date: 2026-08-08

Status: `R4_LOCAL_CORRECTION_COMPLETE_REMOTE_RECHECK_REQUIRED`

## Authority boundary

This record covers only the CI-debt correction lane on draft PR #2485. It does
not issue P8, a lease, credential access, egress, launch, merge, ready-for-review,
Lane A, or any owner-only act. No production source, runtime deadline, or proof
authority is changed by R4.

## Failed remote evidence

GitHub Actions run `31292842333` evaluated PR head
`8d247cc20c97bf5dea94686033e19ca3f732a8a3`.

- Every Ubuntu backend, root, Playwright, migration, release-lock, OCR, Arelle,
  and coverage job passed.
- `dual-gate-windows` reported 355 passes and two failures: phase A and phase B
  of `test_owned_child_boot_go_exit_job_and_inert_authority_contract` each
  raised `dual_live_owned_boot_timeout` before receiving a complete boot frame.
- The evaluator correctly did not run after the first serial command failed.
- `release-gate` failed only as an aggregate consequence. No failed job was
  retried and no production defect is inferred from the hosted-runner timing
  result alone.

## Canonical deadline and R4 correction

The campaign record `2026-08-05-g2-owned-child-logger-freeze-defect.md` defines
the owned-boot window as a hard 5.0 seconds. R4 preserves that production value
and every production control, custody, cleanup, and send-idle deadline.

The correction is test-only:

1. The A/B lifecycle integration test gives only the first owned-boot reader
   wait a 30-second cold-start budget while the child factory is creating a
   process on `windows-latest`.
2. The shared timeout constant remains 5.0 seconds. Any boot-reader cleanup
   join, and all later GO, stop, control, or cleanup operations, retain that
   unchanged deadline.
3. A deterministic blocking-reader test asserts the production value is exactly
   5.0 seconds, forces the timeout path with a short test budget, and proves the
   exact `dual_live_owned_boot_timeout`, reader closure, worker release, and no
   surviving owned-boot reader thread.

Fresh exact-head hosted CI, once accepted below, will prove the lifecycle
contract, not elected-host compliance with the hard 5.0-second startup window.
R4 neither relaxes nor redefines that operational requirement.

## Local verification

Using the pinned Python 3.12 interpreter with authority and credential variables
absent and pytest cache/xdist disabled:

- the timeout seam plus phase A and phase B passed: 3 passed;
- the exact Windows root/strict command passed: 358 passed;
- the separately invoked evaluator command passed: 404 passed;
- no runtime artifact was seeded and no P8, credential, egress, or launch action
  occurred.

## Required acceptance

Before any green-CI or resolved-debt claim, fresh exact-head GitHub Actions must
prove the Windows proof command, the evaluator, every Ubuntu shard, coverage,
and `release-gate` all succeed. A second consecutive code-equivalent workflow
run is required before treating the timing correction as stable.

Until that evidence is recorded, the status remains
`R4_LOCAL_CORRECTION_COMPLETE_REMOTE_RECHECK_REQUIRED` and the PR remains draft.
