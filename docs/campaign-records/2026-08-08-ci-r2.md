# G2-P8 live4 remote CI correction R2

Status: `B_OWNER_PACKET_READY / REMOTE RUN 2 RED / R2 LOCALLY VERIFIED / REMOTE RECHECK REQUIRED / NON-AUTHORIZING`

Recorded: `2026-08-08` local / `2026-08-09` UTC

Task ceiling: `B_OWNER_PACKET_READY`

Lease observation: **NO LEASE EXISTS.** This is not terminal `NO_LEASE_ISSUED`.

Owner fields: **ALL BLANK.** Lane A: **LOCKED.**

This append-only record follows `2026-08-08-ci-remote-correction.md`. It does not rewrite that
record or the earlier local closure record. It binds the second fresh GitHub run and the two exact
defects that remained after the first remote correction.

## 1. Remote subject and outcome

| Binding | Exact value |
|---|---|
| Draft PR | `#2485` / `https://github.com/benjmcd/project6_REPO_MCP_FOLDER/pull/2485` |
| Branch head | `68d598d8eaa09aa0e61d6f04fb3ab56a95c1f4e0` |
| Base | `0b65b4f0b06fdbd1e34460800ef8251cebbb9307` |
| Pull-request merge revision | `3be9ecbf1718104eb01cb5b3630660d92100f130` |
| Second remote run | `31290005351` / `https://github.com/benjmcd/project6_REPO_MCP_FOLDER/actions/runs/31290005351` |
| Result | `completed / failure` |

Passing primary jobs included backend coverage, all four root shards, backend shards 1, 3, and 4,
all four Playwright shards, PostgreSQL migrations and 3C, release-lock installation, NRC OCR, and
SEC Arelle provisioning. `root-tests` and `test` aggregates passed. The `backend-layer3-api` and
`release-gate` aggregate failures were cascades from the two primary failures below.

## 2. Exact primary failures

### Windows checkout

`dual-gate-windows` failed inside `actions/checkout@v6` before Python setup or test execution. The
hosted checkout prefix plus existing tracked planning-document names exceeded the Win32 260-character
boundary. The log reported `Filename too long` for sixteen existing paths. This is not a dual-gate
test failure and no test result is inferred from it.

### Ubuntu backend shard 2

Shard 2 reported `1257 passed, 5 skipped, 1 failed`. The sole failure was:

```text
test_campaign_log_capture.py::test_strict_runner_public_mode_is_reachable_under_reviewed_posture
```

The test passes a POSIX temporary path into the real public wrapper. That wrapper intentionally
requires Windows fixed-volume drive paths and fails closed before reaching the injected runtime
posture probe. The exact node passes on Windows under Python 3.12. Relaxing production path
validation or mocking the preflight would weaken the contract and is rejected.

## 3. R2 correction fence

The implementation correction changes exactly three paths; its containing commit additionally adds
this record as the fourth changed path:

1. `.github/workflows/playwright.yml`
2. `backend/tests/test_campaign_log_capture.py`
3. `backend/tests/test_ci_coverage_completeness.py`

The narrow correction is:

- apply `core.longpaths=true` only to the Windows checkout action through Git's command-scope
  `GIT_CONFIG_COUNT`, `GIT_CONFIG_KEY_0`, and `GIT_CONFIG_VALUE_0` environment interface;
- mark only the real fixed-volume public-path proof as Windows-bound on non-Windows; and
- add that exact backend node to the existing Windows proof command beside the complete root
  dual-gate file.

The configuration is active before checkout, persists no global or system Git state, requires no
administrator action, and does not affect later setup or test steps. The CI guard binds the long-path
configuration, Python 3.12, complete dual-gate file, exact backend proof node, and skip marker.

No production source, runtime, packet, custody, readiness, grant, plan, seal, database, or live4
artifact changes.

## 4. Verification and limitation

Evidence on the R2 candidate:

- new recovery guard intentional RED: `1 failed` because long-path checkout configuration was absent;
- new recovery guard GREEN after the correction: `1 passed`;
- Git command-scope probe: `command / command line: / true` for `core.longpaths`;
- exact backend public-path node on Windows Python 3.12: `1 passed`;
- exact expanded Windows proof command: `357 passed` in `169.26s`;
- the authority/credential/egress environment absence assertion remained active;
- no `PYTHONPYCACHEPREFIX`, secret, credential value, external-endpoint configuration, or live4
  path was added to the implementation paths.

Local Windows evidence cannot prove Ubuntu skip behavior or the hosted checkout. A fresh GitHub run
against the containing commit remains required. No rerun of run `31290005351` is treated as evidence
for changed bytes.

## 5. Required remote recheck

The next run must bind the new branch head and establish all of the following:

1. `dual-gate-windows` completes checkout, runs 357 nodes, and passes;
2. backend shard 2 reports the single fixed-volume node skipped on Ubuntu and no failure;
3. every other previously passing primary job remains passing;
4. `backend-layer3-api`, `root-tests`, and `test` aggregates pass; and
5. the final `release-gate` passes with no hidden primary failure.

Any new red job requires exact-log diagnosis and another bounded correction; an unmodified rerun is
not a substitute.

## 6. State and explicit nonclaims

```text
state=B_OWNER_PACKET_READY
remote_ci_state=SECOND_RUN_RED_R2_PENDING_RECHECK
pr_state=OPEN_DRAFT_CI_VALIDATION_ONLY
owner_fields=ALL BLANK
LEASE_STATE_OBSERVATION=NO LEASE EXISTS
B_OWNER_AUTHORIZED=false
B_CLEARANCE_RECORDED=false
Lane A=LOCKED
```

This record and R2 grant no P8; owner authorization; lease; credential access or placement; egress;
launch, run, retry, or second launch; residual acceptance; PR-ready conversion; review approval;
merge; landing; runtime rebind; terminal disposition; `NO_LEASE_ISSUED`;
`B_CLEARANCE_RECORDED`; remote custody; or Lane A authority. The PR remains draft.
